import React from 'react';
import { Input } from 'semantic-ui-react';

function SearchBar() {
  return (
    <Input action="Search" placeholder="Search..." className="search-bar" />
  );
}

export default SearchBar;
